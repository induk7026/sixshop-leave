spring:
  datasource:
    url: jdbc:h2:file:~/data/holiday

  jpa:
    hibernate:
      ddl-auto: update

---
spring:
  h2:
    console:
      enabled=true:

---
server:
  port: 9090
  error:
    include-exception: true

logging:
  level:
    org.springframework.jdbc.datasource.SimpleDriverDataSource: DEBUG
    org.hibernate.SQL: DEBUG

